# This is a Databricks asset bundle definition for pytest_pyspark.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: pytest_pyspark

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-362282074994262.2.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-362282074994262.2.azuredatabricks.net
